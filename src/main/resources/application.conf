akka {
  loglevel = "DEBUG"
}

email {
  mail = "test"
  mail = ${?EMAIL}
  pwd = "test"
  pwd = ${?EMAIL_PWD}
  unsubUrl = ${UNSUB_URL}
}

facebook {
  token = ${FB_TOKEN}
}

tagsApi {
  host = ${TAGS_HOST}
  port = ${TAGS_PORT}
}